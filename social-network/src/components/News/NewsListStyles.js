import { makeStyles } from "@material-ui/core/styles";
const NewsListStyles = makeStyles((theme) => ({
  border: {
    border: "1px solid #f0f2f5",
    borderRadius: 25,
  },
}));

export default NewsListStyles;
